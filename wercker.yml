box: wercker/nodejs

build:
  steps:
    - npm-install
    - npm-test

deploy:
  steps:
      - script:
          name: Deploy to npm
          code: |
              npm set config email $NPM_AUTHOR_EMAIL
              npm publish .
